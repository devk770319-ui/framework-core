spring:
  config:
    activate:
      on-profile: dev

framework:
  datasource:
    mode: xa
    jndi:
      primary-name: java:comp/env/jdbc/corePrimary
      secondary-name: java:comp/env/jdbc/coreSecondary
